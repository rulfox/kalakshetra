# Base URL of the Spring Boot backend. Server-only — never exposed to the browser, since every
# backend call is proxied through this app's own server (Server Components/Actions/Route
# Handlers), keeping the admin's JWT in an httpOnly cookie the client JS never touches.
BACKEND_API_BASE_URL=http://localhost:8080
