import type { CSSProperties } from 'react';

export const wrap: CSSProperties = { maxWidth: 'var(--container)', margin: '0 auto', padding: '0 var(--gutter)' };
export const section: CSSProperties = { padding: 'var(--space-9) 0' };
