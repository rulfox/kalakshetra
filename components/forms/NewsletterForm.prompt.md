Footer newsletter signup. Validates the email on submit, shows an inline error, then swaps to a serif thank-you confirmation. Self-contained — no props required.

```jsx
<NewsletterForm tone="dark" />
```

Tone defaults to `dark` for the wood footer; use `tone="light"` on ivory surfaces.
