Quiet credibility pill for the Our Story section. Keep these few and factual — no invented metrics.

```jsx
<StatPill value="100%" label="Hand painted" />
<StatPill value="1-of-1" label="Never repeated" />
<StatPill label="Kerala-rooted" />
```

`value` is optional (renders serif gold); `label` is always uppercase + tracked.
